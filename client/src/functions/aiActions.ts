export const decideActions = (units: any) => {

    units.forEach( (u: any) => {
        u.teams.forEach( (t: any) => {
            
            // default order hold is good, as it gives good change to shoot

            // check all opponents and if "t" is inside any
        });
    });
};